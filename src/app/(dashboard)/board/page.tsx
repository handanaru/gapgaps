export default function BoardPage() {
  return (
    <main className="rounded-xl border border-white/10 bg-[#121317] p-6 shadow-lg shadow-slate-950/40">
      <p className="text-[11px] font-medium uppercase tracking-[0.22em] text-cyan-300">Board</p>
      <h1 className="mt-2 text-2xl font-semibold text-white">Board route scaffold</h1>
      <p className="mt-2 max-w-2xl text-sm text-slate-400">
        Step 8에서 기존 Preview Board들을 탭 스위처로 통합할 자리다. 현재는 라우팅 셸만 먼저 준비했다.
      </p>
    </main>
  );
}
